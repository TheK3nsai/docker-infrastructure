# Shared Services - Databases & Cache
# ~/docker/shared-services/docker-compose.yml

services:
  # ============================================
  # PostgreSQL - Zammad, Authentik, future apps
  # ============================================
  postgres:
    image: postgres:16-alpine
    container_name: shared-postgres
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-postgres}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD}
    volumes:
      - postgres-data-16:/var/lib/postgresql/data
      - ./init-scripts/postgres:/docker-entrypoint-initdb.d:ro
    networks:
      - shared-db
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

  # ============================================
  # MariaDB - Nextcloud, future apps
  # ============================================
  mariadb:
    image: mariadb:10.11
    container_name: shared-mariadb
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 512m
        reservations:
          memory: 256m
    command: --transaction-isolation=READ-COMMITTED --binlog-format=ROW
    environment:
      MYSQL_ROOT_PASSWORD: ${MYSQL_ROOT_PASSWORD}
    volumes:
      - mariadb-data:/var/lib/mysql
      - ./init-scripts/mariadb:/docker-entrypoint-initdb.d:ro
    networks:
      - shared-db
    healthcheck:
      test: ["CMD", "healthcheck.sh", "--connect", "--innodb_initialized"]
      interval: 30s
      timeout: 10s
      retries: 5

  # ============================================
  # Redis - Nextcloud, Authentik, future apps
  # ============================================
  redis:
    image: redis:7.4-alpine
    container_name: shared-redis
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256m
        reservations:
          memory: 128m
    command: redis-server --save 60 1 --loglevel warning --databases 16
    volumes:
      - redis-data:/data
    networks:
      - shared-db
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 30s
      timeout: 10s
      retries: 3

  # ============================================
  # Authentik Server
  # ============================================
  authentik-server:
    image: ghcr.io/goauthentik/server:2025.10
    container_name: authentik-server
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 1g
        reservations:
          memory: 512m
    command: server
    environment:
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY}
      AUTHENTIK_REDIS__HOST: shared-redis
      AUTHENTIK_REDIS__DB: 1
      AUTHENTIK_POSTGRESQL__HOST: shared-postgres
      AUTHENTIK_POSTGRESQL__USER: authentik
      AUTHENTIK_POSTGRESQL__PASSWORD: ${POSTGRES_PASSWORD}
      AUTHENTIK_POSTGRESQL__NAME: authentik
    volumes:
      - authentik-media:/media
      - authentik-templates:/templates
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - shared-db
      - traefik-net
    labels:
      - "traefik.enable=true"
      - "traefik.http.routers.authentik.rule=Host(`auth.kensai.cloud`)"
      - "traefik.http.routers.authentik.entrypoints=websecure"
      - "traefik.http.routers.authentik.tls.certresolver=letsencrypt"
      - "traefik.http.services.authentik.loadbalancer.server.port=9000"
      - "traefik.http.routers.authentik.middlewares=security-headers@file"

  # ============================================
  # Authentik Worker
  # ============================================
  authentik-worker:
    image: ghcr.io/goauthentik/server:2025.10
    container_name: authentik-worker
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 768m
        reservations:
          memory: 384m
    command: worker
    environment:
      AUTHENTIK_SECRET_KEY: ${AUTHENTIK_SECRET_KEY}
      AUTHENTIK_REDIS__HOST: shared-redis
      AUTHENTIK_REDIS__DB: 1
      AUTHENTIK_POSTGRESQL__HOST: shared-postgres
      AUTHENTIK_POSTGRESQL__USER: authentik
      AUTHENTIK_POSTGRESQL__PASSWORD: ${POSTGRES_PASSWORD}
      AUTHENTIK_POSTGRESQL__NAME: authentik
    volumes:
      - authentik-media:/media
      - authentik-templates:/templates
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    networks:
      - shared-db

  # ============================================
  # Shared Nginx - Available for future Rails/static apps
  # ============================================
  nginx:
    image: nginx:1.27-alpine
    container_name: shared-nginx
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256m
        reservations:
          memory: 128m
    volumes:
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
      - ./nginx/conf.d:/etc/nginx/conf.d:ro
    networks:
      - shared-db
      - traefik-net
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-net"
      # Shared service (no routes currently - available for future apps)
      - "traefik.http.services.shared-nginx.loadbalancer.server.port=80"

  # ============================================
  # Shared Apache - Reverse proxy for PHP-FPM apps
  # ============================================
  apache:
    image: httpd:2.4-alpine
    container_name: shared-apache
    restart: unless-stopped
    deploy:
      resources:
        limits:
          memory: 256m
        reservations:
          memory: 128m
    volumes:
      - ./apache/httpd.conf:/usr/local/apache2/conf/httpd.conf:ro
      - ./apache/sites-enabled:/usr/local/apache2/conf/sites-enabled:ro
      - invoiceplane-code:/var/www/invoiceplane:ro
    networks:
      - shared-db
      - traefik-net
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=traefik-net"
      # InvoicePlane routing
      - "traefik.http.routers.invoiceplane.rule=Host(`invoices.kensai.cloud`)"
      - "traefik.http.routers.invoiceplane.entrypoints=websecure"
      - "traefik.http.routers.invoiceplane.tls.certresolver=letsencrypt"
      - "traefik.http.routers.invoiceplane.middlewares=authentik@file,security-headers@file"
      - "traefik.http.routers.invoiceplane.service=shared-apache"
      # Shared service
      - "traefik.http.services.shared-apache.loadbalancer.server.port=80"

volumes:
  postgres-data-16:
  mariadb-data:
  redis-data:
  authentik-media:
  authentik-templates:
  invoiceplane-code:
    external: true
    name: invoiceplane_invoiceplane-code

networks:
  shared-db:
    name: shared-db
  traefik-net:
    external: true
