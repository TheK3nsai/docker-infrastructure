# NetBox Configuration
NETBOX_VERSION=v4.4-3.4.2

# Database (PostgreSQL via shared-services)
NETBOX_DB=netbox
NETBOX_DB_USER=netbox
NETBOX_DB_PASSWORD=changeme

# Django secret key (generate with: python3 -c "import secrets; print(secrets.token_urlsafe(50))")
NETBOX_SECRET_KEY=changeme

# Superuser credentials
NETBOX_SUPERUSER=admin
NETBOX_SUPERUSER_EMAIL=admin@kensai.cloud
NETBOX_SUPERUSER_PASSWORD=changeme
